# FBG Interrogator Control Software

Настольное приложение под Windows 10 для управления оптоволоконным интеррогатором
**FBG-Interrogator GC-97001C-03-01-A-F** (2000 Гц · 4 канала · прошивка 4.10 · SN 94401220).

Производитель прибора — Zhuhai Arcadia Optronix.

---

## Состояние

| Модуль | Статус | Коммит | Тесты |
|---|---|---|---|
| `fbg/core/profile.py` | ✅ сверен со скринингом | чат №5 | входит в 133 теста кодека |
| `fbg/core/frames.py` | ✅ сверен со скринингом | чат №5 | входит в 133 теста кодека |
| `fbg/core/codec.py` | ✅ готов, 15 команд | чат №5 | 133 |
| `fbg/core/endpoint.py` | ✅ готов | чат №5 | входит в 46 тестов транспорта |
| `fbg/core/transport.py` | ✅ готов | чат №3 | 46 + 3 нагрузочных |
| `fbg/core/session.py` | ✅ Р62: чтения/записи разрешены в Streaming | чат №11 | 59 |
| `fbg/core/pipeline.py` | ✅ снимки UI + длинное окно Р65 | чат №12 | 36 + тесты измерения + нагрузочные |
| `fbg/core/calibration.py` | ✅ готов | чат №9 | 46 |
| `fbg/io/packet_log.py` | ✅ готов | чат №8 | 46 + 4 нагрузочных |
| `fbg/io/recorder.py` | ✅ готов | чат №7 | 35 + 3 нагрузочных |
| `fbg/io/config.py` | ✅ готов | чат №9 | 49 |
| проводка журнала к сессии | ✅ готова | чат №9 | 12 |
| `fbg/sim/encode.py` | ✅ сверен со скринингом | чат №5 | входит в 75 тестов симулятора |
| `fbg/sim/scene.py` | ✅ сверен со скринингом | чат №5 | входит в 75 тестов симулятора |
| `fbg/sim/device_sim.py` | ✅ Р62/Р63 воспроизводятся в симуляторе | чат №11 | 78+ |
| `fbg/ui/app.py` · сборка, профиль, запись и спектр | ✅ готов | чат №13 | контроллер + интеграционные тесты |
| `fbg/ui/models.py` · `texts.py` · `diagnostics.py` | ✅ модели UI без Qt | чат №13 | настройка, измерение и спектр |
| `fbg/ui/main_window.py` · панели | ✅ подключение, измерение, спектр, прибор, настройка, журнал | чат №13 | `ui` |
| `main.py` | ✅ точка входа | чат №10 | — |
| `fbg/ui/panels/device_config.py` | ✅ настройка прибора | чат №11 | модель без Qt + `ui` |
| `fbg/ui/panels/measurement.py` | ✅ график λ(t), таблица, запись | чат №12 | модель без Qt + `ui` |
| `fbg/ui/panels/spectrum.py` | ✅ 30 07: single + периодический снимок; 30 03 доступен контроллеру | чат №13 | модель без Qt + `ui` |

Чат №12 добавляет отдельные Qt-свободные тесты модели измерения, `ui`-тесты
панели и совместный нагрузочный прогон потока, записи, журнала и графика.

Чат №13 добавляет вкладку **«Спектр»**. Основной путь — `30 07` выбранного
канала: одиночный снимок по кнопке либо периодические отдельные снимки с
заданным периодом; рядом показывается эквивалентная частота обновления.
Периодический режим — явное расширение базового Р64 по требованию оператора,
а не изменение факта о протоколе. Это не параллельный live-поток ADC: каждая команда группы
`0x30` является режимной. Если до снимка шла телеметрия, приложение выполняет
полный цикл `Stop → 30 07 → Stop → опрос конфигурации → Start`; следующий
периодический цикл не начинается, пока предыдущий не закончился. Во время
активного `Recorder` спектр запрещён, а запуск записи запрещён при работающем
периодическом спектре — диагностическая команда не может молча разорвать файл.

Ось X строится в нанометрах через `DeviceProfile.adc_index_to_nm`: индекс 0 —
Stop и λ убывает к последнему отсчёту. Ось Y переключается между сырыми ADC и
дБм по подтверждённой N6 формуле; тёмное смещение 68 и коэффициенты уровней
усиления находятся в `DeviceProfile`. Области ищутся как непрерывные участки
`ADC >= threshold`, поэтому плато насыщения является одной областью. Для
ненасыщенной области показываются вершина, центроид, амплитуда, ширина и FWHM;
при ADC=16383 положение вершины, центроид и FWHM намеренно не рассчитываются.
`AppSnapshot.last_spectrum_max_adc` и `last_spectrum_saturated_points` теперь
заполняются последним снимком и доступны панели настройки.

> ⚠️ Тесты интерфейса проверяют **устройство** окна: вкладки на месте, таймер
> стартует и гаснет, кнопки включаются по состоянию сессии, таблица журнала
> не держит кольцо. Как окно выглядит — вопрос визуальной приёмки, и тестами
> он не подменяется.

## Что изменил скрининг 27.08.2026

Ядро приведено в соответствие с захватом (чат №5). Три правки изменили
численный результат разбора:

| Что | Было | Стало | Вопрос |
|---|---|---|---|
| База развёртки | 196251 (PDF) | **196250**, поле `sweep_base_ghz` | D8 |
| Единицы частоты | автодетект по кадру | **делитель 10** по умолчанию | D1 |
| «Пик не найден» | только валидация по диапазону | **сырое `0x000000`** | N3 |
| Раскладка `30 03` | тело отдавалось сырым | `[Канал(2) Усиление(2) ADC×2551] × 4` | N14 |
| Ориентация АЦП | индекс 0 = Start | **индекс 0 = Stop**, λ убывает | D9 |
| Команд в протоколе | 14 | **15**: добавлена `10 02` | N17 |

Заводские параметры прибора (1 / 5101) под новой базой дают 196249 / 191149,
а не круглые границы: прибор хранит параметр, а не частоту.

> ✅ Сессия 1 подтвердила полные длинные ответы: `30 07` = 5112 байт в четырёх
> датаграммах, `30 03` = 20430 байт в четырнадцати. В `screening4.pcapng`
> запросов `30 07`/`30 03` нет, поэтому для новой пяти-решёточной линии полного
> реального ADC×2551-вектора нет; тест пяти областей остаётся явно помеченной
> реконструкцией по реальным положениям телеметрии.

> ✅ Скрининг 30.08 и 01.09.2026 закрыл D4, N5, N6, N9, N10, N12, N17,
> N18 и N19. Для чата №11 ключевой результат — Р62: во время потока группы
> `0x10` и `0x20` работают без остановки телеметрии, а режимная группа `0x30`
> поток вытесняет. После `30 07` простой повтор `30 02` поток не вернул (Р63).
> Открытым для отказоустойчивости остаётся N11 — поведение после обрыва линка.

> ⚠️ Команда `10 02` (N17) реализована сборкой и разбором, но **не включена**
> ни в пробник, ни в watchdog: скрининг показал, что ответ постоянен, но смысл
> двух ненулевых полей по-прежнему неизвестен и на рабочий тракт не влияет.

## Что изменил скрининг 01.09.2026

* **Р62.** В `Streaming` разрешены чтения `0x10` и записи `0x20`. `10 04`,
  `10 06` и `20 02` прошли при 2000 Гц без измеримой потери кадров.
* **Р63 / R13.** `30 07` остановил поток через 43 мс; повторный `30 02` его
  не вернул. Поэтому сессия запрещает группу `0x30` в `Streaming`, кроме Stop.
* **D4.** `20 06` ответа не имеет и сохраняет только пороги: порог пережил
  отключение питания, усиление вернулось в авто, уровень 5. Read-back `10 06`
  подтверждает только текущее совпадение порогов, но не запись во flash.
* **R14.** Прибор не проверяет номер канала и может испортить настройки
  реальных каналов. Номер для записи берётся только из живого ответа `10 04`;
  порог жёстко ограничен `0…16383`, режим `FFFF` доступен только как «авто».
* Порог выше максимума спектра даёт ноль пиков. На стенде максимум был 11123,
  и порог 12000 дал пустой кадр; автоусиление уровней 3–4 насыщало АЦП.

---

## Настройки и калибровки

Настройки лежат в `fbg_config.json` рядом с исполняемым файлом: ни базы, ни
реестра. Отсутствие файла ошибкой не является — приложение стартует на
умолчаниях и создаёт файл при первом сохранении. Файл, испорченный правкой
руками, приложение не роняет: битый JSON, чужая кодировка, неизвестное поле,
поле неверного типа и значение, отвергнутое проверкой, дают умолчание
и **названное** замечание, а не отказ старта. Опечатка при этом стоит одного
поля, а не всего раздела.

В файле стоит версия формата. Файл версии новее текущей не читается вовсе
и при сохранении откладывается в `*.bad`, а не затирается: частичное чтение
файла из будущего опаснее умолчаний, потому что незнакомые поля пропали бы
молча.

Из `DeviceProfile` в настройки попадает не всё, и разделение идёт по признаку
«кто владелец числа»:

| Владелец | Поля | В файле |
|---|---|---|
| прибор (`10 04`, `10 05`) | `channels`, `fbg_per_channel`, `sweep_speed_hz`, `peak_gap_ghz`, параметры развёртки | да, кэшем последнего опроса |
| скрининг | `sweep_base_ghz`, `freq_divisor`, `case_temp_scale`, `peak_missing_codes`, `adc_index_ascending_freq`, `mode_len_width`, `set_sweep_*` | нет |
| оператор | `case_temp_signed` (вопрос N2b открыт) | да |

Кэш геометрии нужен потому, что кольцо истории, буферы кадра и фильтр
телеметрии журнала строятся из профиля **до** подключения. Обновляет его
`profile_from_device` после успешного опроса конфигурации.

Поля второй строки настройкой не являются: их изменение означает, что прибор
**другой**, и правильный путь — новый скрининг и новые умолчания в
`profile.py`. Вписанное в файл руками такое поле игнорируется с явной причиной.
Довод — асимметрия цены ошибки: `sweep_base_ghz`, сдвинутый на единицу, даёт
систематические 8 пм, невидимые ни в одном числе на экране.

Калибровки — отдельный JSON. Датчик описывается **ожидаемой длиной волны
и окном поиска**, а не номером позиции в кадре (Р30): позиции — это слоты,
заполняемые по возрастанию λ по мере обнаружения, и на стенде 1551.5 нм лежит
в позиции 1, будучи четвёртой решёткой линии. Окно покрывает рабочий диапазон
датчика, а не паспортный разброс решётки: FBG даёт ≈10 пм/°C, и рабочие
−15…+55 °C требуют ±0.35 нм.

Исходов поиска три, и все различимы: один пик в окне — значение; ни одного —
`NaN`; **больше одного — тоже `NaN`**, а не ближайший из них. «Ближайший» —
монетка: если решётка датчика пропала, а в окне остался соседний пик, вернулась
бы чужая длина волны под именем этого датчика. Пересечение окон двух датчиков
одного канала ловится один раз при загрузке набора.

⚠️ Формула температурной компенсации — **гипотеза** (вопрос N22): из таблицы
`FBGInfo` штатного ПО известны только имена полей `TC_Coff` и `TC_Base`.

Идентификация прибора (`serial`, `firmware`) хранится в настройках один раз
и подставляется в шапки файла данных и журнала. Известна она только после
опроса конфигурации, поэтому в первом файле журнала первого запуска стоит
честное `unknown`: журнал создаётся раньше подключения, чтобы записать сам
обмен подключения.

---

## Приложение

```bash
python main.py
```

Пять вкладок: **Подключение**, **Измерение**, **Прибор**, **Настройка**, **Журнал пакетов**.

Логика отделена от виджетов не ради стройности, а ради проверяемости: сборка
приложения (`fbg/ui/app.py`), модели панелей (`models.py`), тексты (`texts.py`)
и лестница диагностики (`diagnostics.py`) **не импортируют Qt** и проверяются
без экрана. Это закреплено дымовым тестом, который запускает импорт в отдельном
интерпретаторе — внутри общего прогона живут тесты, тянущие Qt намеренно.

Панели читают **снимок** (`AppController.snapshot()`) по таймеру 10 Гц и ничего
из ядра не держат. Событийного обновления нет: журнал видит каждую датаграмму,
и при 2000 Гц отрисовка по событию утопила бы UI.

### Первое, что делает приложение при подключении

Сверяет геометрию прибора (ответы `10 04` и `10 05`) с профилем из настроек.
`Session` профиль не обновляет намеренно — он нужен и без живого прибора, при
переразборе журнала, — поэтому `profile_from_device` зовёт приложение.

Совпало — идентификация прибора уезжает в настройки, и со следующего запуска
шапки файлов верны с первого байта. Разошлось — **не сохраняется ничего**, ни
профиль, ни серийный номер, а расхождение показывается построчно. Принять
геометрию прибора можно только явным действием, при закрытой связи и
остановленной записи: кольцо истории, буфер кадра и фильтр телеметрии журнала
уже выделены под старый профиль, поэтому смена геометрии пересобирает
компоненты.


### Настройка прибора

Панель редактирует порог и усиление выбранного физического канала, интервал
пиков и развёртку, а также отправляет `20 06` для сохранения порогов. Все
записи `0x20` проходят через существующий контракт сессии: подтверждение
команды ещё не считается успехом — после него выполняется read-back, а
расхождение остаётся в `Session.unconfirmed` и показывается как
**«не подтверждено»**.

Номер канала нельзя набрать вручную: список строится из числа каналов живого
ответа `10 04`. Это защита от R14, а не удобство интерфейса — прибор принимает
несуществующие номера и при скрининге такая запись испортила реальные
настройки. Порог вручную ограничен `0…16383`; `FFFF` представлен отдельным
флажком **«Авто»** и никогда не проходит через числовое поле. Усиление имеет
режим авто/вручную и уровень `0…5`.

Перед `20 01` панель считает число точек АЦП и показывает будущие границы в
ГГц и нм. Нарушение `start_param < stop_param` блокирует отправку. После первого снимка спектра рядом с порогом и усилением показываются максимум
ADC и число насыщенных точек последнего спектра; до первого снимка остаётся `—`.
Порог выше показанного максимума закономерно даст ноль найденных областей.

Все команды `0x20` протокольно разрешены и во время `Streaming` по Р62.
Порог, усиление, интервал и сохранение порогов не останавливают поток.
Развёртка тоже отправляется как разрешённая `20 01`, но после подтверждённого
изменения приложение останавливает сессию и пересобирает pipeline, журнал и
сессию под новые границы `DeviceProfile`; для продолжения нужно подключиться
снова. Иначе телеметрия проверялась бы по старому диапазону. Режимные команды
`0x30` сессия блокирует, кроме `30 01`. Кнопки запуска/остановки потока остаются
на панели подключения; управление записью измерений здесь не появляется.

Кнопка **«Сохранить пороги»** подписана буквально: `20 06` сохраняет только
пороги. После команды сессия выдерживает задержку и читает `10 06`; совпадение
означает только, что текущие значения те же. Энергонезависимую запись можно
доказать лишь отключением питания.

### Измерение и запись

Панель **«Измерение»** обновляется общим таймером окна 10 Гц. Таблица показывает
30 позиций каждого из четырёх каналов: длину волны и отдельный признак
валидности, а температуры корпуса выводятся по каналам. Позиции остаются
слотами телеметрии, а не именами датчиков; переменное число заполненных слотов
между соседними кадрами считается штатным поведением.

График рисует только отмеченные пользователем позиции. UI не получает ссылку
на `RingHistory`: `Pipeline.trace_history()` делает отдельный неизменяемый снимок
только выбранных столбцов за запрошенное время. Максимальная видимая история
ограничена ёмкостью кольца: 20 000 кадров дают 10 секунд при 2 кГц. NaN
сохраняется в массиве и передаётся pyqtgraph с `connect="finite"`, поэтому
пропавший пик даёт разрыв, а не интерполяцию.

Абсолютные длины волн около 1550 нм плохо подходят для автомасштаба пикометровых
изменений. Поэтому каждая линия показывается как **Δλ относительно первого
валидного значения в текущем видимом окне**. Абсолютная λ остаётся в таблице.
Диапазон Y считается только по конечным значениям выбранных линий, с запасом
10 % и минимальным окном 0.002 нм, чтобы ровный сигнал не схлопывал шкалу.

Управление записью находится на этой же панели. До старта показывается оценка
объёма на 10 минут для текущей заполненности и верхняя оценка для полностью
заполненных выбранных позиций; изменение децимации или лимита позиций сразу
пересчитывает прогноз. Во время записи видны файл, число строк, объём и время.
Ошибка диска остаётся на панели после остановки `Recorder` и не останавливает
приём. Если курсор отстал, статистика `# GAP` показывается явным предупреждением.

Состояние записи берётся только из `AppSnapshot.recording`. Поэтому Р67 не имеет
второго флага: на панели настройки развёртка блокируется тем же состоянием,
которым панель измерения включает кнопку остановки записи. Порог и усиление при
этом остаются доступны во время `Streaming` по Р62. Метрика темпа и оценка
потерь считаются на длинном окне до 10 секунд (Р65), а не по мгновенным пачкам.

### Диагностика подключения

Панель подключения показывает не «ошибка подключения», а лестницу из KB_01,
половину которой закрывают счётчики транспорта:

| Вердикт | Значение |
|---|---|
| `[ok]` | проверено кодом, претензий нет |
| `[!!]` | проверено кодом, вероятная причина отказа |
| `[ ? ]` | кодом не проверяется, делает человек |

`commands_sent > 0` при `datagrams_received == 0` — команды уходят, ответов
нет, то есть закрытый входящий UDP/8001 либо молчащий прибор.
`foreign_datagrams > 0` — отвечает кто-то другой. `icmp_resets > 0` — прибор
недоступен. Приём не на `0.0.0.0` — единственный пункт, проверяемый полностью:
это молчание при полностью исправной связи, и выглядит оно как «сеть
не работает».

### Порядок остановки

`Stop` прибору → сессия → recorder → журнал → pipeline. Сначала замолкает
источник, потом закрывается связь, потом писатели дописывают хвосты — им нужно
живое кольцо, — и последним гаснет тракт. Каждый шаг идёт в своём `try`: отказ
одного не оставляет прибор льющим поток.

### Подключение не блокирует окно

`Probing` — это `Stop` плюс пять чтений, каждое с таймаутом и повторами: на
молчащем приборе окно замерло бы на секунды. Поэтому `connect` уходит в обычный
`threading.Thread`, а результат забирает тот же таймер 10 Гц, которым UI и так
читает снимки. `QThread` поверх наших потоков не появляется: второго слоя
управления жизненным циклом здесь нет.

---

## Стек

Python 3.12 · PySide6 · pyqtgraph · numpy · pytest · PyInstaller

Разбор байтов — только `struct` и `int.from_bytes`.
Сеть — блокирующий `socket` в отдельном потоке, без asyncio.
`pandas` в тракте приёма данных запрещён.

---

## Установка

```bash
python -m venv .venv
.venv\Scripts\activate        # Windows
pip install -e ".[dev]"
```

---

## Тесты

```bash
pytest                        # всё, кроме требующего прибора
pytest -m "not ui and not slow"   # ядро, как в CI
QT_QPA_PLATFORM=offscreen pytest -m ui   # интерфейс без экрана
pytest -m slow                # нагрузочные тесты 2 кГц на loopback
pytest -m device              # только при подключённом приборе
```

Тесты с маркером `ui` требуют PySide6; без него они пропускаются, а не падают,
поэтому job ядра в CI живёт в окружении вовсе без Qt.

Критерий нагрузочных тестов: симулятор шлёт 2000 кадров/с по 494 байта,
потери менее 0.1 % за 60 секунд.

* `tests/test_load.py` — сторона **отправки**: выдерживает ли симулятор темп.
* `tests/test_transport_load.py` — сторона **приёма**: доходят ли датаграммы
  до потребителя через `UdpTransport`, с разбором кодеком и без.
* `tests/test_pipeline_load.py` — **весь тракт**: симулятор → транспорт →
  сессия → pipeline → курсор писателя.
* `tests/test_recorder_load.py` — **тракт до файла**: то же плюс запись CSV
  на диск, с проверкой полноты по номерам кадров в самих файлах.
* `tests/test_packet_log_load.py` — **журнал в полном режиме**: симулятор →
  транспорт → `packet_log`, с замером объёма и проверкой того, что журнал
  не создаёт обратного давления на приём.
* `tests/test_ui_measurement_model.py` (`slow`) — **одновременная работа**:
  симулятор → сессия → pipeline → recorder при активном штатном журнале и
  построении модели четырёх кривых около 10 раз/с. Проверяются нулевые потери
  транспорта, отсутствие `# GAP` и отставания журнала.

Измерено на loopback (Linux, 60 с × 2000 Гц): 119 945 отправлено, 119 945
дошло до потребителя, потери 0.0000 %, пик очереди 45 из 4096, межкадровый
интервал p50 499 мкс · p99 593 мкс · p99.9 795 мкс. Тот же прогон с полным
разбором каждого кадра — тоже без потерь.

Сквозной прогон через `pipeline` (Linux, 60 с × 2000 Гц): отправлено 120 001,
доехало до писателя 120 001, потери 0.0000 %, разрывов у писателя нет. Пик
очереди транспорта 39 из 4096, пик отставания писателя 59 кадров из 20 000,
кольцо 19.4 МБ. Задержка приёмного потока (от `recvfrom` до конца разбора)
p50 104 мкс · p99 241 мкс · p99.9 1108 мкс; задержка доставки писателю
p50 5.2 мс · p99 10.1 мс — в ней сидит его собственный опрос раз в 10 мс.
UI при этом обновлялся 19.9 раза в секунду, а не 2000: децимация по времени.

Короткий совместный прогон чата №12 (10 с × 2000 Гц, штатный журнал):
20 272 кадра отправлено и 20 272 разобрано, очередь транспорта не вытеснила
ни одного кадра, `Recorder` не получил ни одного разрыва, журнал не потерял
записей. Модель четырёх кривых обновилась 96 раз; длинное окно дало 1999.88 Гц.
Это проверка обратного давления в коде; внешний вид графика остаётся визуальной
приёмкой на Windows.

Запись на диск (Linux, 60 с × 2000 Гц, чат №7): отправлено 120 001, строк
в файлах 120 001, потерь и разрывов нет, 63.1 МБ в двух файлах (ротация
сработала на ходу), строка в среднем 551 Б при двух распознанных решётках.
Файл целиком ASCII, включая шапку: `numpy.genfromtxt` открывает его
в локальной кодировке ОС, и на Windows это cp1252. Порядок частей записи
восстанавливается алфавитной сортировкой имён и полем `file_part` в шапке,
но не временем создания файла: на Windows его разрешение грубее интервала
между ротациями.
Собственный потолок записи на худшем случае — все 120 позиций заполнены,
строка 1254 Б — 32 800 строк/с и 39 МБ/с, из них 83 % времени уходит
на форматирование. Запас над паспортными 2000 Гц — шестнадцатикратный,
поэтому децимация нужна ради объёма файлов, а не ради темпа.

Журнал пакетов (Linux, 60 с × 2000 Гц, чат №8): в полном режиме
120 004 записи, 177.3 МБ в трёх файлах, 2.95 МБ/с, запись в среднем 1549 Б.
Потери на приёме 0.0000 %, пик очереди транспорта 32 из 4096, журнал
не вытеснил ни одной записи — обратного давления на приём он не создаёт,
потому что `log_rx` только кладёт байты в свою очередь, а форматирование
и файл живут в отдельном потоке. Собственный потолок журнала — 134 000
записей/с и 198 МБ/с, поровну между форматированием и записью, запас
над паспортным темпом более чем полусотенный.

По умолчанию телеметрия в журнал **не пишется**: те же 15 секунд потока
дают файл в несколько сотен байт вместо 44 МБ. Полнота включается явно —
`telemetry_stride=1` — и по умолчанию ограничена 20 000 кадров, то есть
десятью секундами потока. Промежуточный режим `stride=N` стоит ровно
во столько раз меньше: при `stride=10` объём 0.30 МБ/с против 2.95 МБ/с,
отношение 10.0.

Файл журнала, как и файл измерений, целиком ASCII: его открывают Блокнотом
и импортируют в Excel, а кодировку по умолчанию там выбирает ОС. Поэтому
метки расшифровки английские (`Telemetry ch=4 filled=2`), а не переведённые.

---

## Как ведётся разработка

Проект развивается по одному модулю за раз. Каждый рабочий чат:

1. Клонирует репозиторий и фиксирует базовый коммит.
2. Пишет модуль вместе с тестами.
3. Отдаёт результат как патч в формате `git format-patch`.

Применение патча:

```bash
git pull
git am -3 chatNN_module.mbox
pytest
git push
```

Флаг `-3` обязателен: он включает трёхстороннее слияние и позволяет применить
патч, даже если ветка ушла вперёд.

Если патч не применился:

```bash
git am --abort
```

и вернуться в координационный чат с текстом ошибки.

---

## Проверки

| Уровень | Где | Что проверяет |
|---|---|---|
| 1 | Локально перед пушем | `ruff check` + `pytest` |
| 2 | GitHub Actions | То же на ubuntu и windows + интерфейс offscreen + нагрузочный тест |
| 3 | Координационный чат | Клон HEAD, прогон тестов, сверка с базой знаний |

---

## База знаний

Спецификация протокола, профиль прибора, архитектурные решения и журнал
открытых вопросов ведутся отдельно в файлах `KB_01`…`KB_06`.

**Приоритет источников:** реальные захваты трафика → файлы `KB_*` → PDF
производителя. В PDF производителя найдено не менее восьми фактических ошибок,
поэтому он используется как гипотеза, а не как факт.

---

## Лицензия

Внутренний проект.
